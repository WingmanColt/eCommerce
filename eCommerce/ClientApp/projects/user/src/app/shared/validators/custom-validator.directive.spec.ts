import { CustomValidatorDirective } from './custom-validator.directive';

describe('CustomIdValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
